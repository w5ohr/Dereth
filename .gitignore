# OS cruft
.DS_Store

# Claude Code local (per-user) settings — keep launch.json shared, ignore local overrides
.claude/settings.local.json

# server runtime data (SQLite accounts/characters) — never commit player data
server/*.db
__pycache__/
*.pyc
